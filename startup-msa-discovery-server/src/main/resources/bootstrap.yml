spring:  
  profiles:    active: local
  cloud:    config:      enabled: false

eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: false